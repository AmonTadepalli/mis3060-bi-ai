# HW2 Validation — Wildcat Capital Transaction Portfolio

## 2A — Known-Answer Benchmarks

| Check | Expected | Your Script Produced | Match? | Notes |
|---|---|---|---|---|
| Dataset shape | (298772, 9) | (298772, 9) | Yes | |
| Null count — `security_id` | 101,597 | 101,597 | Yes | |
| Null count — `amount` | 0 | 0 | Yes | |
| Unique `txn_type` values | 6 | 6 | Yes | |
| Count of `Buy` transactions | 83,556 | 83,556 | Yes | |
| `txn_date` data type | object | object | Yes | |
| Earliest `txn_date` | 2020-01-01 | 2020-01-01 | Yes | |
| Latest `txn_date` | 2024-12-30 | 2024-12-30 | Yes | |
| Duplicate `txn_id` count | 0 | 0 | Yes | |
| Mean `amount` | $54,075.17 | $54,075.17 | Yes | |
| Median `amount` | $41,220.48 | $41,220.49 | No | 1-cent difference. TODO: investigate — likely a rounding/interpolation difference between pandas' `.median()` on the even-count series vs. how the benchmark was calculated independently. Paste your Cowork investigation here. |
| Skewness of `amount` | 1.15 | 1.15 | Yes | |
| Correlation `shares`–`amount` | 0.65 | 0.65 | Yes | |
| Correlation `price`–`amount` | 0.64 | 0.64 | Yes | |
| Correlation `shares`–`price` | 0.00 | 0.00 | Yes | |
| Negative `shares` count (Buy only) | 836 | 836 | Yes | |
| Profile file created | Yes | Yes | Yes | `hw02/hw02_profile.txt` |
| Chart files created (3) | Yes | Yes | Yes | `hist_amount.png`, `box_amount_by_type.png`, `scatter_shares_amount.png` in `hw02/charts/` |

---

## 2B — Explain the Code and Output

*Complete this section in a NEW Claude Cowork session — not the one that generated the script.*

**Prompt 1 (the code):** [paste hw02_eda.py, then ask: "Walk me through each section of this script, including the grouping, correlation, and charting steps. What should I see in the terminal when I run it? List each expected output value explicitly."]

**Prompt 2 (the output):** [paste the full terminal output, then ask: "Here is the terminal output from running an EDA script on a wealth management transaction dataset. What does each value mean? Flag anything that looks unexpected or that I should investigate before using this data in an analysis."]

1. Did Claude's predicted outputs (from Prompt 1) match what you actually saw in the terminal? List any discrepancies.

   When I compared the predicted output to what my script actually produced, almost everything lined up exactly, since the walkthrough was generated by running the script itself rather than guessing at values. The one discrepancy I caught: the median amount was predicted as $41,220.48 but my script actually returned $41,220.49, a one-cent difference. I think this comes down to how the benchmark's median was calculated separately versus pandas' .median() method.

2. What did Claude flag as potentially unexpected or worth investigating (from Prompt 2)?

   Two things got flagged as worth investigating. First, the Advisory Fee group's mean ($7,375.17) is about 9 times its median ($859.12), which is a much bigger gap than any other transaction type shows, meaning a handful of unusually large fee transactions are skewing the average. Second, 836 of my Buy transactions have negative shares (as low as -499.63), while Sell and Dividend transactions have zero negative values, which seemed inconsistent for a category that should represent acquiring shares.

3. Did Claude mention the 101,597 null values in `security_id`? What explanation did it give?

   Yes, the 101,597 nulls in security_id got explained as structural rather than random missing data. They line up with the Deposit, Withdrawal, and Advisory Fee rows, which don't reference a security in the first place, so those fields are expected to be empty for those transaction types.

4. Did Claude flag the `txn_date` column as a concern? Why would that matter for a time-series analysis?

   No, txn_date wasn't flagged as a concern initially, just reported as a date range from 2020-01-01 to 2024-12-30. But looking at the schema output again, txn_date is stored as dtype object (a string), not an actual date. That matters for time-series analysis because pandas can't sort dates chronologically, calculate the days between transactions, or resample by time period using strings, I'd need to convert the column with pd.to_datetime() first, and any inconsistent formatting would only show up once I did that.

5. Open your three chart files. Does what you see in each image match Claude's explanation of that section of the output? Note any differences.

   Opening the three chart files: the histogram shows a right-skewed distribution of amount with the mean line sitting to the right of the median line, matching the 1.15 skewness value. The box plot shows six boxes ordered by ascending median, with Advisory Fee sitting noticeably lower and tighter than the rest with a long tail of outliers, which lines up with the mean/median gap I noticed in question 2. The scatter plot shows a general positive relationship between shares and amount, matching the 0.65 correlation, and the Buy-colored points extend into negative share values on the left side, which are the 836 negative-share rows.

6. Paste one follow-up question you asked Claude, and Claude's answer.

   Prompt: For the 836 negative-share Buy transactions, are they concentrated among specific advisors or securities or spread evenly across the dataset?

   Response: I ran the numbers to answer it against your actual file:

   Negative-share Buys hit almost every advisor at close to the same rate, roughly 1.0-1.3% of that advisor's Buy transactions, with the highest at 1.29% and lowest around 1.01%, no single advisor stands out as the source. By security, the top securities each show only 5 negative-share rows out of 500 securities total, again no concentration. And the negative rows span the full date range (2020-01-02 to 2024-12-28), not clustered around one period like a bad data load or a specific system migration.

   That even spread across advisors, securities, and time is a strong signal this isn't a real operational issue (like one advisor's team using a broken order-entry workflow), it looks like uniform random noise baked into the dataset, consistent with this being a synthetic/generated dataset for the assignment rather than a live firm's data. If this were a real book of business, I'd treat evenly-distributed negative shares as more likely a sign convention or data-entry artifact than a business event, and I'd want to know how the dataset was generated before deciding whether to filter, flag, or leave those rows as-is for analysis.

---

## 2C — Business Check & Cross-Validation

**Business-reasonableness questions** (answer in your own words — do not paste Claude's response here):

1. `security_id`, `shares`, and `price` are all null in exactly 101,597 rows. Looking at the `txn_type` value counts, which three transaction types would you expect to have no security — and why? Do the counts add up to 101,597?

   I would expect Deposits, Withdrawls, and Advisory Fees to have no security information because they are cash or account-level transactions rather than purchases or sales of a specific investment. They do add up to 101,597, since deposits were at 35,981, withdrawls at 29,850, and advisory fees at 35,766.

2. There are 83,556 Buy transactions and 59,755 Sell transactions. What does it mean for a wealth management firm to have significantly more Buys than Sells over a five-year period?

   I would interpret the higher number of Buys as a possible sign that clients were generally adding money and investing it during the five-year period. It could also reflect portfolio growth or reinvestment. However, I could not conclude that the firm purchased more in total dollar value because these are transaction counts, not transaction amounts. The firm could have made many small purchases and fewer, larger sales.

3. The `txn_date` column is stored as a string (type `object`) rather than a date. If Claude Cowork generated code to compute the average number of days between transactions, what would go wrong if the dates remained as strings?

   If the dates remained strings, the code would not be able to calculate time differences correctly. Strings are treated as text, so subtracting one date string from another would either produce an error or lead to incorrect comparisons. I would first convert txn_date to a proper datetime type before calculating the number of days between transactions.

4. Wildcat Capital has 2,700 clients served by 25 advisors. Is that ratio — roughly 108 clients per advisor — plausible for a registered investment advisory firm?

   I think a ratio of approximately 108 clients per advisor is plausible for a registered investment advisory firm. Advisors often use technology and support staff to manage many client relationships. However, whether the ratio is reasonable would depend on the firm’s service model. It would be more manageable for clients receiving standardized services than for ultra-high-net-worth clients who require highly personalized attention.

5. 836 `Buy` transactions have negative `shares` values (as low as −499.63), while every other transaction type in the dataset has only positive share values. What are two plausible business explanations for a negative share count on a Buy transaction, and what would you do next to determine which explanation is more likely?

   One possible explanation is that the negative Buy transactions are reversals or corrections of earlier purchases. For example, if a Buy was entered incorrectly, the firm might record a negative Buy to cancel all or part of it. Another possibility is a data-entry or system-mapping error, where the shares received the wrong sign or a Sell transaction was incorrectly labeled as a Buy. To determine which explanation is more likely, I would review the source system’s transaction rules and examine the affected transactions for patterns. I would look for matching positive Buy transactions involving the same client, security, number of shares, and nearby dates. Matching transactions would support the reversal explanation. If no matches exist and the negative values appear randomly, a data-quality or classification error would be more likely.

**Cross-validation:**

- Prompt A: "Write Python to count rows in fact_transactions.csv where txn_type equals exactly 'Buy'."
- Prompt B: "Write Python to count the total rows in fact_transactions.csv, then subtract the count of rows where txn_type is Sell, Deposit, Withdrawal, Dividend, or Advisory Fee."

6. What did each script return?

   Script A (a direct filter) returned 83,556. Script B (298,772 - 215,216 non-Buy rows) also returned 83,556.

7. Do the results agree? If not, which one is wrong and why?

   Yes, the results do agree. The two scripts use different logic, one matching "Buy" directly, and the other inferring it by exclusion. Both getting the same number proves it isn't a one-off.

8. Why is it useful to verify a count using subtraction rather than direct filtering?

   Direct filtering only tests whether I'm matching the string 'Buy' correctly. Subtraction tests something different, it forces me to get every other category and the total row count right. If the two disagreed, that mismatch would point to an error I might otherwise miss.
